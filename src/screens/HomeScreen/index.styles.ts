import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    padding: 12,
  },
  intent: {
    paddingBottom: 24,
  },
  footer: {
    margin: 8,
    alignSelf: 'center',
  },
  hint: {
    fontSize: 12,
  },
});
